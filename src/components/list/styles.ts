import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingBottom: 20,
  },
  cardMargin: {
    marginBottom: 16,
  },
  loader: {
    marginVertical: 20,
  },
});